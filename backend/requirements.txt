# Pinned to the versions verified in production (2026-07). Bump deliberately +
# re-test; unpinned deps mean every deploy could pull a breaking/compromised release.
fastapi==0.128.8
uvicorn[standard]==0.39.0
google-genai==1.47.0
numpy==2.0.2
pydantic==2.13.4
slowapi==0.1.9
