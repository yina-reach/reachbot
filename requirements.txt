# Ingest pipeline deps (reachin_export.py, zoom_transcripts.py, ingest.py).
# The app runtime deps live in backend/requirements.txt (FastAPI) and
# frontend/package.json (Next.js).
google-genai
numpy
pyyaml
notion-client
python-slugify
requests
