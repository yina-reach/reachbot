.env
__pycache__/
*.pyc
.DS_Store
index_partial.npz

# Scraper dedup cache — contains transient S3 presigned URLs (embedded AWS creds).
# Regenerated by fetch_external_links.py; must never be committed.
reachin_md/_external_seen.json
